# Golden fixtures

CLI output that is intentionally stable should be added here as minimized fixtures. Timing-bearing JSON is not golden-tested because timings are environment-dependent; structural JSON fields are tested in `tests/cli.rs`.
